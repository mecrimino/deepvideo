# Deep Vision — Python core (core/) dependencies.
# Install:  pip install -r requirements.txt
# Heavy local models (torch/CLIP) are intentionally NOT required — the core is
# cloud-first (Ch20) and falls back to a hashed embedder + cloud services.

fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2   # settings/.env loader (separate package in pydantic v2)
loguru>=0.7              # structured logging — used by every module at import
httpx>=0.27
python-dotenv>=1.0
numpy>=1.26
pillow>=10.0
psutil>=5.9              # Developer Dashboard performance metrics

# Optional — the app boots and generates without these (they load lazily):
# faster-whisper>=1.0     # local speech-to-text for uploaded audio (heavy: ctranslate2)
# chromadb>=0.5           # vector memory / RAG (else a hashed embedder is used)
# langgraph>=0.2          # full Ch19 WorkflowEngine (the mini pipeline ships its own)
# langchain-core>=0.3     # message types used by the LangGraph agents
# opencv-python-headless  # local vision / shot detection (Ch13/Ch14)
