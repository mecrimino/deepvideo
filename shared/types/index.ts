/**
 * Frontend data types — the editor's EDL/timeline model plus the (now
 * backend-less) run and API contracts. Imported throughout the app as
 * `@deep-video/shared` (aliased to this file in vite.config.ts + tsconfig).
 */

export * from './edl.js';
export * from './pipeline.js';
export * from './api.js';
