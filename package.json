{
  "name": "deep-vision",
  "version": "0.1.0",
  "private": true,
  "description": "Deep Vision \u00e2\u20ac\u201d AI video creation studio. Monorepo: frontend (React editor), backend (API), core (Python agent pipeline), shared types.",
  "workspaces": [
    "frontend",
    "backend",
    "motion"
  ],
  "scripts": {
    "dev": "npm run dev -w @deep-vision/frontend",
    "dev:backend": "npm run dev -w @deep-vision/backend",
    "dev:core": "python -m uvicorn core.main:app --host 127.0.0.1 --port 8000 --reload",
    "build": "npm run build -w @deep-vision/frontend",
    "typecheck": "npm run typecheck -w @deep-vision/frontend && npm run typecheck -w @deep-vision/backend"
  },
  "engines": {
    "node": ">=20"
  }
}